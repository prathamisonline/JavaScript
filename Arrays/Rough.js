const months = ["jan", "feb", "mar", "apr"];
const cashflows = [
  { month: "jan", value: 10 },
  { month: "mar", value: 20 },
];

const newArray = [];
function combination(month, cashflows) {
  newArray.map();
}

console.log(newArray);
